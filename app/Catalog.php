<?php

namespace App;

use Illuminate\Database\Eloquent\Model;

class Catalog extends Model
{
    /*public function getKindAttribute()
    {
        $i = 1;
        foreach (config('code_geek.kind') as $k) {
            if ($i == $this->attributes['kind'])
                return config('code_geek.kind.' . $i);
            $i++;
        }
        return 'Other';
    }
    public function getTypeAttribute()
    {
        $i = 1;
        foreach (config('code_geek.kind') as $k) {
            if ($i == $this->attributes['kind'])
                return config('code_geek.kind.' . $i);
            $i++;
        }
        return 'Other';
    }
    public function getKindAttribute()
    {
        $i = 1;
        foreach (config('code_geek.kind') as $k) {
            if ($i == $this->attributes['kind'])
                return config('code_geek.kind.' . $i);
            $i++;
        }
        return 'Other';
    }*/
}
